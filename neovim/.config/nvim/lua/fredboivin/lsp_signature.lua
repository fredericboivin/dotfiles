require"lsp_signature".setup(
    {
        bind = true,
        handler_opts = {border = "rounded"},
        verbose = false,
        floating_window = true,
        hint_enable = false,
        -- hint_prefix = " ",
        -- change color hint help
        -- hint_scheme = "String",
    }
)
